# Логгер температуры

Кроссплатформенная программа на C++ для считывания температуры с последовательного порта и записи в лог-файлы с разной политикой хранения.

## 📋 Требования ТЗ

- Считывание температуры с последовательного порта (реального или виртуального)
- Три лог-файла:
  - `temperature_raw.log` — все измерения за последние 24 часа
  - `temperature_hourly.log` — средние значения за час (последний месяц)
  - `temperature_daily.log` — средние значения за день (текущий год)
- Симулятор устройства для тестирования без реального железа

## ⚙️ Сборка

```
mkdir build
cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
make -j$(nproc)
```
## Запуск
Терминал 1: создание виртуальных портов
```bash
socat -d -d pty,raw,echo=0 pty,raw,echo=0
# Запомните порты из вывода, например: /dev/pts/5 и /dev/pts/6
```
Терминал 2: симулятор
```
./simulator /dev/pts/5 9600
```
Терминал 3: логгер
```
./logger /dev/pts/6 9600
```

## Важно
Для работы требуется установленный пакет socat:
```
sudo apt install socat
```
